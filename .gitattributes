# Normalise line endings to LF in the repo across platforms (silences the
# "LF will be replaced by CRLF" checkout warnings on Windows).
* text=auto eol=lf

# Binary assets — never touch line endings.
*.png binary
*.woff binary
*.woff2 binary
*.ttf binary
